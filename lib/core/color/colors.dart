import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  static const white = Colors.white;
  static const black = Color(0xff000000);
  static const randomColor = Color(0xffbbffee);
}
